<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <title>Admin Dashboard</title>
    <style>
        * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
        }
        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            height: 100vh;
            background-color: #f5f5f5;
        }
        .sidebar {
            width: 20%;
            background-color: #253947;
            color: white;
            padding: 20px;
        }

        .logo-container {
            display: flex; /* Display logo and text side by side */
            align-items: center; /* Align them vertically */
            margin-bottom: 20px; /* Add some space below */
        }

        .logo {
            width: 50px; /* Adjust the size of the logo */
            height: 50px;
            margin-right: 10px; /* Add space between logo and text */
        }

        .logo img {
            width: 100%; /* Make the image fill the container */
            height: auto;
            border-radius: 50%; /* Make it circular if you prefer */
        }

        .sidebar h2 {
            font-size: 18px; /* Adjust the font size as needed */
            margin: 0; /* Remove default margins */
        }

        .sidebar ul {
            list-style-type: none;
        }

        .sidebar ul li {
            padding: 10px 0;
            font-size: 18px;
        }

        .sidebar ul li a {
            color: white; /* Text color remains white */
            text-decoration: none;
            display: block;
            padding: 10px;
            border-radius: 5px; /* Optional: Adds rounded corners */
        }

        .sidebar ul li a:hover {
            background-color: #3e5062; /* Darker hover effect */
        }

        .content {
            background-color: white;
            width: 80%;
            padding: 20px;
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            grid-gap: 20px;
        }

        /* Center the content of the box */
        .content .box {
            background-color: #e0e0e0;
            padding: 25px;
            border-radius: 10px;
            text-align: center;
            height: 200px;
            display: flex; /* Add this to enable flexbox */
            flex-direction: column; /* Stack items vertically */
            justify-content: center; /* Center items vertically */
            align-items: center; /* Center items horizontally */
        }

        /* Make the total user number bigger */
        .total-users-number {
            font-size: 48px; /* Adjust the size as needed */
            font-weight: bold;
            color: #333; /* Customize the color */
        }

        .box h3 {
            margin-bottom: 10px; /* Add spacing between title and number */
            font-size: 24px; /* Adjust the title font size */
        }

        .appointment-section {
            grid-column: span 2;
            display: grid;
            grid-template-columns: 1fr 1fr;
            grid-gap: 20px;
        }

        .appointment-box {
            background-color: #e0e0e0;
            padding: 15px; /* Reduced padding */
            border-radius: 10px;
            height: 500px; /* Fixed height for better alignment */
        }

        .appointment-box h3 {
            margin-bottom: 10px;
        }

        .appointment-box table {
            width: 100%;
            border-collapse: collapse;
        }

        .appointment-box table th, .appointment-box table td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #ccc;
        }
    </style>
</head>
<body>
    
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo-container">
            <div class="logo">
                <img src="images/logo.png" alt="Logo"> <!-- Path to the logo -->
            </div>
            <h2>Workforce Alliance Inc.</h2>
        </div>
        <ul>
            <li><a href="/admindashboard">Dashboard</a></li>
            <li><a href="/appointment">Appointment</a></li>
            <li><a href="/message">Message</a></li>
            <li><a href="/history">History</a></li>
        </ul>
    </div>


    <!-- Main Content -->
    <div class="content">
        <!-- Reduced the size of the boxes here -->
        <div class="box">
            <h3>Total Users</h3>
            <div class="total-users-number"><%= totalUsers %></div> <!-- Number will be bigger -->
        </div>        
        <div class="box">
            <h3>Appointment</h3>
        </div>
        <div class="box">
            <h3>Message</h3>
        </div>

        <!-- Appointment Section -->
        <div class="appointment-section">
            <div class="appointment-box">
                <h3>Appointment</h3>
                <table>
                    <tr>
                        <th>Name</th>
                        <th>Status</th>
                    </tr>
                    <tr>
                        <td>John Doe</td>
                        <td>Pending</td>
                    </tr>
                    <!-- Add more rows as needed -->
                </table>
            </div>
            <div class="appointment-box">
                <h3>Today's Appointment</h3>
                <table>
                    <tr>
                        <th>Name</th>
                        <th>Status</th>
                    </tr>
                    <tr>
                        <td>Jane Smith</td>
                        <td>Confirmed</td>
                    </tr>
                    <!-- Add more rows as needed -->
                </table>
            </div>
        </div>
    </div>

</body>
</html>